function sumar(num1, num2) {
    const resultado = num1 + num2;
    console.log(`La suma de ${num1} más ${num2} es igual a ${resultado}`);
}

// Ejemplo de uso
sumar(5, 3);
function restar(num1, num2) {
    const resultado = num1 - num2;
    console.log(`La resta de ${num1} menos ${num2} es igual a ${resultado}`);
}

// Ejemplo de uso
restar(5, 3);
